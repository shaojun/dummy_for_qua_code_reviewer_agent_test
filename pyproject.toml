[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "myapp"
version = "0.1.0"
description = "A simple dummy Python project for PR testing"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
